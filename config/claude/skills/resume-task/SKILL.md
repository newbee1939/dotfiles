---
name: resume-task
description: 中断していたタスクを再開するときに使う。チケットの内容と現状の進捗を確認し、次にやることを提示する。
---

# Resume Task（タスク再開）

1. **チケットの内容と進捗を確認する**: Linear のチケット本文・最新コメント（`mcp__linear*`）、`git status` / `git log`、`gh pr view`（あれば）。
2. **現状と次にやることを短く提示する**。

**勝手に作業を始めない**。ユーザーの指示を待つ。
